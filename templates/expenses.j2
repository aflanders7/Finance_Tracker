<table>
    <thead>
    <tr>
        <th>Name</th>
        <th>Amount Spent</th>
        <th>Category</th>
        <th>Description</th>
        <th>Date</th>
    </tr>
    <tbody>
    {% for item in Expenses %}
    <tr>
        {% for key in item %}
        <td>{{key}}</td>
        {% endfor %}
    </tr>
    {% endfor %}
</table>

<div id="insert">
		<form id="Add_Expense" action="/expenses" method="post">
			<legend><strong>Add an expense</strong></legend>
			<fieldset>
                <!-- placeholder shows ghost text, required forces an input, pattern forces input adherence -->
				<label for="name">Name:</label>
				<input type="text" class="form-control" id="name" name="name"  placeholder="" required>

				<label for="amount">Amount Spent:</label>
				<input type="number" class="form-control" id="amount" name="amount" max="2000" placeholder="" required>

				<label for="category">Category:</label>
				<select class="form-control" id="category" name="category" placeholder="" required>
					<option value="Food">Food</option>
					<option value="Living">Living</option>
					<option value="Transportation">Transportation</option>
					<option value="Health">Health</option>
                    <option value="Entertainment">Entertainment</option>
				</select>

				<label for="description">Desrciption:</label>
				<input type="text" class="form-control" id="description" name="description" placeholder="">

                <label for="date">Date:</label>
                <input type="date" id="date" name="date" value="2023-11-01" />
            </fieldset>

            <!-- Create 'Add Person' and 'Cancel' buttons -->
			<input type="submit" value="Add_Expense" name="Add_Expense" class="btn btn-primary" style="margin:.5rem;">

        </form>
</div><!--insert-->
